---
interface Props {
  title: string;
  titleEn?: string;
  description: string;
  descriptionEn?: string;
  date: string;
  tags: string[];
  slug: string;
  locale?: 'pt' | 'en';
}
const { title, titleEn, description, descriptionEn, date, tags, slug, locale } = Astro.props;
const localePrefix = locale ? `/${locale}` : '';
---
<article class="blog-card">
  <span class="blog-card__date">{date}</span>
  <a href={`${localePrefix}/blog/${slug}`} class="blog-card__title"
    transition:name={`blog-title-${slug}`}
    data-i18n-pt={title}
    data-i18n-en={titleEn || title}
  >{title}</a>
  <p class="blog-card__desc"
    data-i18n-pt={description}
    data-i18n-en={descriptionEn || description}
  >{description}</p>
  <div class="tags" style="margin-top: 0.5rem">
    {tags.map(t => <span class="tag">{t}</span>)}
  </div>
</article>
